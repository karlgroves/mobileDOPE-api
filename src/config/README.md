# Configuration Files

This directory contains configuration files for the Mobile DOPE API.

## Files

- `database.ts` - Database connection configuration
- `jwt.ts` - JWT authentication configuration
- `cors.ts` - CORS policy configuration
- `logger.ts` - Bunyan logger configuration
- `cache.ts` - Redis cache configuration

All configuration should read from environment variables with sensible defaults.
