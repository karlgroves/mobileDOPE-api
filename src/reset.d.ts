// Improves TypeScript's built-in types (e.g. `.filter(Boolean)`, `JSON.parse`,
// `.includes()` on literal unions). See https://github.com/total-typescript/ts-reset
import '@total-typescript/ts-reset';
