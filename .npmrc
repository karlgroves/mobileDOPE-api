save-exact=true
engine-strict=true

# Wait 7 days before resolving a newly published version. A package compromised
# at publish time is usually caught and yanked within days, so a short quarantine
# removes most of that window at no real cost. Affects resolution only --
# `npm ci` installs exactly what package-lock.json pins and is unaffected.
min-release-age=7
