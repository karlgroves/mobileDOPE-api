# =====================================================================
# Mobile DOPE API - Environment Configuration
# =====================================================================
# Copy this file to .env and update with your values
# DO NOT commit .env to version control

# =====================================================================
# Server Configuration
# =====================================================================
NODE_ENV=development
PORT=3000

# =====================================================================
# Database Configuration (MySQL 9.x)
# =====================================================================
# For Docker Compose development, use: db
# For local development, use: localhost
DB_HOST=db
DB_PORT=3306
DB_NAME=mobile_dope_dev
DB_USER=api_user
DB_PASSWORD=dev_password_changeme

# Production database (Digital Ocean Managed MySQL)
# DB_HOST=your-db-host.db.ondigitalocean.com
# DB_PORT=25060
# DB_NAME=mobile_dope_prod
# DB_USER=doadmin
# DB_PASSWORD=your-production-password

# =====================================================================
# JWT Configuration
# =====================================================================
# IMPORTANT: Change JWT_SECRET in production to a secure random string
# Generate with: openssl rand -base64 32
JWT_SECRET=dev_jwt_secret_changeme_in_production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# =====================================================================
# CORS Configuration
# =====================================================================
# Comma-separated list of allowed origins
# For React Native, include exp:// URLs for Expo development
CORS_ORIGIN=http://localhost:3001,http://localhost:8081,exp://192.168.1.100:8081

# =====================================================================
# Rate Limiting
# =====================================================================
# Window in milliseconds (900000 = 15 minutes)
RATE_LIMIT_WINDOW_MS=900000
# Maximum requests per window per user
RATE_LIMIT_MAX_REQUESTS=1000

# =====================================================================
# Logging
# =====================================================================
# Options: error, warn, info, debug
LOG_LEVEL=debug

# =====================================================================
# Email Configuration (for verification and password reset)
# =====================================================================
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=your-smtp-password
# SMTP_FROM=Mobile DOPE <noreply@example.com>

# =====================================================================
# File Storage (for target images)
# =====================================================================
# Options: local, s3
# STORAGE_PROVIDER=local
# STORAGE_PATH=./uploads

# AWS S3 Configuration (if using S3)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
# AWS_REGION=us-east-1
# AWS_S3_BUCKET=mobile-dope-uploads

# =====================================================================
# Redis Configuration (optional - for caching and sessions)
# =====================================================================
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=
# REDIS_DB=0

# =====================================================================
# Application URLs
# =====================================================================
# Frontend URL for email links
FRONTEND_URL=http://localhost:3001
# API Base URL
API_URL=http://localhost:3000

# =====================================================================
# Security
# =====================================================================
# Enable/disable registration
ENABLE_REGISTRATION=true
# Require email verification
REQUIRE_EMAIL_VERIFICATION=true
# Password reset token expiry (in hours)
PASSWORD_RESET_EXPIRY=1
# Email verification token expiry (in hours)
EMAIL_VERIFICATION_EXPIRY=24
